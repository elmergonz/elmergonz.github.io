export class Data {
    /**
     * @param {Array<Array>} rows - Array of rows
     * @param {Array<Array>} cols - Array of columns
     */
    constructor(rows, cols) {
        this.rows = rows;
        this.cols = cols;
    }
}
